# БЫСТРЫЙ СТАРТ — Pach Group

## 🚀 Запуск проекта за 10 минут

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
# Telegram Bot (обязательно!)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Admin (обязательно!)
JWT_SECRET=your-very-secure-secret-key-min-32-characters-long
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password

# Next.js
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**Как получить Telegram токен:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

**Как получить Chat ID:**
1. Добавьте бота в чат/группу
2. Откройте [@userinfobot](https://t.me/userinfobot)
3. Скопируйте свой ID

**Сгенерировать JWT_SECRET:**
```bash
# В терминале (Linux/Mac):
openssl rand -base64 32

# Или онлайн:
# https://generate-secret.vercel.app/32
```

---

### 3. Запуск dev-сервера

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

---

### 4. Вход в админку

1. Прокрутите главную страницу вниз
2. В Footer нажмите "Вход" (справа внизу)
3. Введите логин и пароль из `.env.local`
4. Вы в админ-панели!

**Или напрямую:** [http://localhost:3000/admin/login](http://localhost:3000/admin/login)

**По умолчанию:**
- Логин: `admin`
- Пароль: `admin123` (измените в `.env.local`!)

---

## ✅ ЧТО СДЕЛАТЬ ПЕРЕД ЗАПУСКОМ

### Минимум (30 минут):

1. **Заменить контент** (`src/data/mock.ts`):
   - [ ] Реальные объекты (минимум 6)
   - [ ] Реальные сделки
   - [ ] Реальные отзывы

2. **Загрузить изображения**:
   - [ ] Поместить фото в `/public/images/properties/`
   - [ ] Обновить `imageUrl` в `mock.ts`

3. **Убрать тестовые данные**:
   - [ ] Footer: удалить "Демонстрационный сайт (MVP)"
   - [ ] Footer: удалить ссылку "Вход"

4. **Проверить контакты**:
   - [ ] Телефоны в Footer
   - [ ] Адрес офиса
   - [ ] Ссылки на Telegram/Instagram

---

## 🔧 ОСНОВНЫЕ ФАЙЛЫ

**Контент:**
- `src/data/mock.ts` — Объекты, сделки, отзывы

**Компоненты:**
- `src/components/Header.tsx` — Шапка
- `src/components/Hero.tsx` — Главный экран
- `src/components/Listings.tsx` — Каталог объектов
- `src/components/Contacts.tsx` — Форма контактов
- `src/components/Footer.tsx` — Подвал

**Админка:**
- `src/app/admin/login/page.tsx` — Страница входа
- `src/app/admin/page.tsx` — Админ-панель

**API:**
- `src/app/api/contact/route.ts` — Отправка заявок в Telegram
- `src/app/api/auth/login/route.ts` — Вход в админку

---

## 📦 СБОРКА ДЛЯ ПРОДАКШЕНА

```bash
# Сборка
npm run build

# Запуск продакшен-сервера
npm start
```

**Деплой на Netlify:**
1. Подключите GitHub репозиторий
2. Build command: `npm run build`
3. Добавьте переменные окружения (Settings → Environment variables)
4. Deploy!

---

## 🆘 ЧАСТЫЕ ПРОБЛЕМЫ

**Проблема:** Форма не отправляется в Telegram  
**Решение:** Проверьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` в `.env.local`

**Проблема:** Не могу войти в админку  
**Решение:** Проверьте `ADMIN_USERNAME` и `ADMIN_PASSWORD` в `.env.local`

**Проблема:** Изображения не загружаются  
**Решение:** Проверьте URL изображений в `mock.ts` или пути к локальным файлам

**Проблема:** 3D-модель не отображается  
**Решение:** Это нормально на некоторых устройствах/браузерах (fallback предусмотрен)

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

Полный аудит проекта: `PROJECT_STATUS.md`

**Разделы:**
- Что реализовано
- Что нужно добавить
- Чеклист перед запуском
- Итоговая оценка

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После запуска MVP:

1. Подключить базу данных (Supabase)
2. Улучшить безопасность (хеширование паролей)
3. Добавить аналитику (Google Analytics)
4. Расширенное SEO (sitemap.xml)
5. Фильтры и поиск объектов

---

**Удачи с запуском! 🚀**

Если что-то не работает — проверьте `PROJECT_STATUS.md` для детальной информации.

